rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /docs/{userEmail}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write, delete: if request.auth != null && request.auth.token.email == userEmail;
    }
  }
}
